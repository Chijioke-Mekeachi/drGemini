# --- Frontend Configuration ---

# The base URL for the backend API.
# If you are using the Vite proxy, you can leave this as /api
# Otherwise, set it to the full backend URL, e.g., http://localhost:5001/api
VITE_API_BASE_URL=/api
